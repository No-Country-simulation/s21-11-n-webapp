spring:
  application:
    name: bakesoft
  datasource:
    url: jdbc:mysql://localhost/bakesoft
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  logging:
    level:
      org.springframework.mail: DEBUG