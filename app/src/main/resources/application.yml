spring:
  application:
    name: bakesoft
  datasource:
    url: jdbc:mysql://${DB_URL}/${DB_NAME}
    username:  ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
  logging:
    level:
      org.springframework.mail: DEBUG